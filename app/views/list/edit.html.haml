.listeditPage
  .container
    = form_with model: @list, url: { action: :update }, html: { class: 'listForm' }, local: true do |f|
      - if @list.errors.any?
        %p.text-danger タイトルは1～255文字以内で入力してください
      = f.label :title, "あなたが普段行っていること"
      = f.text_field :title, class: "form-control listName", placeholder: "リスト名"
      .listnewForm_time_zone
        .time 普段行っていることの時間帯
        = f.check_box :time_zone, {multiple: true},"朝"
        朝
        = f.check_box  :time_zone, {multiple: true},"昼"
        昼
        = f.check_box  :time_zone, {multiple: true},"夕方"
        夕方
        = f.check_box  :time_zone, {multiple: true},"夜"
        夜
      .text-center= f.submit "編集する", class: "submitBtn"
